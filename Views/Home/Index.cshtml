@{
    ViewData["Title"] = "Home Page"; //This is what displays in the tab on the webpage (Home Page- Data_Dashboard)
}

@{await Html.RenderPartialAsync("Graph1");}

@*@{await Html.RenderPartialAsync("Graph2", "Home");}

@{await Html.RenderPartialAsync("Graph3", "Home");}*@

